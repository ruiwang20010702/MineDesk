# 🎯 MineDesk 下一步行动计划

> **当前状态**：MineContext 后端 ✅ | LLM 配置 ✅ | 前端待集成 🟡 | Screenpipe 待启动 🟡

---

## 📅 阶段 1：验证与熟悉（本周内，1-3 天）

### 目标
让现有系统真正"跑起来"，验证核心功能，为后续开发建立信心。

### 任务清单

#### 1.1 测试 MineContext 核心功能 ⭐⭐⭐

**为什么重要**：验证 LLM 配置效果，熟悉 API 能力

**具体操作**：
```bash
# 运行完整演示
cd "/Users/ruiwang/Desktop/killer app"
./demo_siliconflow.sh

# 使用命令行工具
source MineContext_Commands.sh
help        # 查看所有命令
status      # 检查服务状态
test_api    # 测试 API
```

**测试场景**：
- ✅ 简单对话："你好，介绍一下你自己"
- ✅ 知识问答："什么是 RAG？它如何工作？"
- 🔲 **文档摄入**：导入 MineDesk PRD（见 1.2）
- 🔲 **语义检索**：搜索 PRD 中的内容
- 🔲 **多轮对话**：带上下文的连续对话

**预期结果**：
- API 响应速度 < 3s
- 中文对话质量优秀
- 文档检索准确率 > 80%

---

#### 1.2 导入测试文档 ⭐⭐

**为什么重要**：验证 RAG 管道（检索增强生成）的完整链路

**具体操作**：
```bash
# 使用 curl 导入 MineDesk PRD
curl -X POST http://127.0.0.1:17860/api/ingest/document/write \
  -H "Content-Type: application/json" \
  -d @- << 'EOF'
{
  "documentId": "minedesk_prd_v1.6",
  "source": "local",
  "mimeType": "text/markdown",
  "title": "MineDesk 产品需求文档 v1.6",
  "createdAt": "2025-11-03T20:00:00Z",
  "content": "$(cat 'MineDesk/MineDesk_PRD_v1.6.md')",
  "metadata": {
    "project": "MineDesk",
    "version": "v1.6",
    "type": "PRD"
  }
}
EOF

# 测试检索
curl -X POST http://127.0.0.1:17860/api/agent/chat \
  -H "Content-Type: application/json" \
  -d '{
    "query": "MineDesk 的核心架构是什么？包含哪些模块？",
    "sessionId": "prd-test-001"
  }'
```

**或者批量导入您的其他文档**：
- 工作笔记
- 会议记录
- 代码文档
- 研究资料

**验证**：
- 文档是否成功入库
- 检索时能否召回相关片段
- LLM 生成的答案是否准确

---

#### 1.3 浏览 API 文档 ⭐

**为什么重要**：了解 MineContext 的全部能力

**访问**：http://127.0.0.1:17860/docs

**重点关注的 API**：
- `/api/agent/chat` - 智能对话（已测试）
- `/api/ingest/document/write` - 文档摄入
- `/api/search/text` - 文本检索
- `/api/search/vector` - 向量检索
- `/api/model_settings/*` - LLM 配置管理
- `/api/debug/*` - 调试接口

**实验**：
- 在 Swagger UI 中直接测试各个接口
- 尝试不同的参数组合
- 查看返回的数据结构

---

#### 1.4 查看运行日志（可选）

```bash
# 实时查看日志
tail -f /tmp/minecontext.log

# 查看最近 100 行
tail -100 /tmp/minecontext.log

# 搜索错误
grep -i "error" /tmp/minecontext.log | tail -20
```

---

## 📅 阶段 2：启动 Screenpipe（本周或下周，2-4 天）

### 目标
启动桌面活动捕获，建立"记忆层"，实现上下文采集。

### 2.1 检查 Screenpipe 状态

```bash
cd "/Users/ruiwang/Desktop/killer app/screenpipe-main"
ls -la

# 查看是否已编译
ls -la target/release/screenpipe 2>/dev/null || echo "需要编译"
```

### 2.2 编译并启动 Screenpipe

**如果未编译**：
```bash
cd "/Users/ruiwang/Desktop/killer app/screenpipe-main"

# 安装 Rust（如果没有）
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh

# 编译（可能需要 10-20 分钟）
cargo build --release

# 启动
./target/release/screenpipe
```

**配置**：
- 授予屏幕录制权限（macOS 系统偏好设置）
- 授予麦克风权限（可选）
- 配置红区遮罩（隐私保护）

### 2.3 验证 Screenpipe 数据

```bash
# 查看数据库
sqlite3 ~/.screenpipe/db.sqlite "
  SELECT COUNT(*) AS total_frames FROM frames;
  SELECT COUNT(*) AS total_ocr FROM ocr_text;
"

# 查看最近的活动
sqlite3 ~/.screenpipe/db.sqlite "
  SELECT timestamp, app_name, window_name 
  FROM frames 
  ORDER BY timestamp DESC 
  LIMIT 10;
"
```

### 2.4 集成到 MineContext

**方案 A**：直接查询 Screenpipe SQLite
```python
# 在 MineContext 中添加 Screenpipe 数据源
import sqlite3
screenpipe_db = "~/.screenpipe/db.sqlite"
# 查询并摄入到 MineContext
```

**方案 B**：定时同步
```bash
# 创建定时任务，每小时同步一次
# 从 Screenpipe 提取 → 转换 → 写入 MineContext
```

---

## 📅 阶段 3：前端开发（2-4 周）

### 目标
开发 Electron 桌面应用，提供用户界面。

### 3.1 评估现有前端

MineContext 已经包含一个 Electron 前端：
```bash
cd "/Users/ruiwang/Desktop/killer app/MineContext-main/frontend"
ls -la
```

**决策**：
- **选项 A**：使用 MineContext 的前端作为基础（推荐）
  - 已有屏幕捕获功能
  - 已有数据库集成
  - 需要定制 UI 和功能
  
- **选项 B**：从零开发新前端
  - 完全按照 MineDesk PRD 设计
  - 更符合愿景
  - 开发周期更长

### 3.2 启动前端开发（如果选择 A）

```bash
cd "/Users/ruiwang/Desktop/killer app/MineContext-main/frontend"

# 安装依赖
npm install

# 启动开发模式
npm run dev
```

**定制任务**：
- [ ] 修改 UI 样式和布局
- [ ] 添加快捷键（Cmd+Shift+Space）
- [ ] 集成 MineContext API
- [ ] 添加设置界面
- [ ] 实现会话管理

### 3.3 核心功能实现

根据 PRD，优先实现：
1. **快捷触发**：全局快捷键唤起
2. **智能对话**：连接到 MineContext API
3. **上下文展示**：显示检索到的文档片段
4. **截图 OCR**：选区识别（已有基础）
5. **设置面板**：LLM 配置、隐私设置

---

## 📅 阶段 4：CrewAI 集成（3-5 周）

### 目标
实现多智能体协作，自动生成周报。

### 4.1 安装 CrewAI

```bash
cd "/Users/ruiwang/Desktop/killer app"
/opt/homebrew/bin/python3.11 -m pip install crewai crewai-tools
```

### 4.2 创建 Weekly Report Crew

参考 PRD 的设计：
- **Researcher Agent**：检索上下文
- **Analyst Agent**：提取关键指标
- **Writer Agent**：生成报告
- **Reviewer Agent**：校对
- **Exporter Agent**：导出

```python
# weekly_report_crew.py
from crewai import Agent, Task, Crew, Process

# 定义 Agents
researcher = Agent(
    role='Context Researcher',
    goal='Retrieve relevant context from the past week',
    backstory='Expert at finding relevant information',
    llm_config={'base_url': 'https://api.siliconflow.cn/v1', ...}
)

analyst = Agent(
    role='Data Analyst',
    goal='Extract key metrics and insights',
    backstory='Skilled at data analysis'
)

# 定义 Tasks
research_task = Task(
    description='Find all activities from the past week',
    agent=researcher
)

# 组装 Crew
crew = Crew(
    agents=[researcher, analyst, ...],
    tasks=[research_task, ...],
    process=Process.sequential
)

# 运行
result = crew.kickoff()
```

### 4.3 连接数据源

```python
# 从 MineContext 检索
# 从 Screenpipe 查询
# 合并上下文
```

---

## 📅 阶段 5：MCP 平台（4-7 周）

### 目标
集成外部系统（Notion、GitHub、Jira 等）。

### 5.1 实现 MCP 适配器

```python
# mcp_adapters/notion_adapter.py
class NotionAdapter:
    def __init__(self, api_key):
        self.client = NotionClient(auth=api_key)
    
    def create_page(self, database_id, properties):
        # 创建 Notion 页面
        pass
    
    def sync_to_minecontext(self):
        # 同步到 MineContext
        pass
```

### 5.2 支持的集成

按优先级：
1. **Notion** - 笔记和知识库
2. **GitHub** - 代码和 Issues
3. **Slack** - 团队沟通
4. **Jira** - 项目管理
5. **飞书** - 企业协作

---

## 📅 阶段 6：跨平台适配（6+ 周）

### 目标
扩展到 Windows 和 iOS。

### 6.1 Windows 适配

- 替换屏幕捕获 API
- 调整快捷键注册
- 测试兼容性

### 6.2 iOS 精简版

- SwiftUI 界面
- 远程 API 调用
- Siri Shortcuts 集成

---

## 🎯 当前最优先任务（本周必做）

### ✅ 已完成
- [x] Python 3.11 升级
- [x] MineContext 部署
- [x] LLM 配置（硅基流动）
- [x] 基础测试

### 🔲 待完成（优先级排序）

1. **⭐⭐⭐ 运行完整演示**
   ```bash
   ./demo_siliconflow.sh
   ```
   **时间**：5 分钟
   **价值**：验证所有配置

2. **⭐⭐⭐ 导入测试文档**
   - 导入 MineDesk PRD
   - 导入您的工作笔记
   - 测试 RAG 检索
   **时间**：30 分钟
   **价值**：体验完整的知识问答

3. **⭐⭐ 启动 Screenpipe**（可选，但建议）
   - 编译并运行
   - 授予权限
   - 查看采集数据
   **时间**：1-2 小时
   **价值**：建立记忆层

4. **⭐⭐ 浏览前端代码**
   ```bash
   cd MineContext-main/frontend
   code .  # 在 VS Code 中打开
   ```
   **时间**：1 小时
   **价值**：了解前端架构

5. **⭐ 性能测试**
   - 压力测试 API
   - 测试大文档摄入
   - 监控资源占用
   **时间**：1 小时
   **价值**：了解系统边界

---

## 📊 项目路线图对照

根据 MineDesk PRD v1.6 的 30 周路线图：

| 阶段 | PRD 规划 | 当前状态 | 进度 |
|------|----------|----------|------|
| M0 (1-2周) | 架构设计 | ✅ 已完成 | 100% |
| M1 (3-5周) | macOS 原型 + screenpipe | 🟡 进行中 | 60% |
| M2 (6-9周) | Graph + KB MVP | 🟡 MineContext 已就绪 | 80% |
| M2.5 (9-10周) | SQLite-VSS 升级 | 🔲 待开始 | 0% |
| M3 (10-13周) | 隐私中心 | 🔲 待开始 | 0% |
| M4 (14-17周) | weekly_report | 🔲 待开始 | 0% |
| M5 (18-21周) | MCP 平台 | 🔲 待开始 | 0% |
| M6 (22-26周) | CrewAI + Windows | 🔲 待开始 | 0% |
| M7 (27-30周) | iOS 版本 | 🔲 待开始 | 0% |

**当前位置**：M1 阶段后期 / M2 阶段早期

---

## 🔧 快速参考

### 常用命令
```bash
# 服务管理
source MineContext_Commands.sh
status      # 查看状态
restart     # 重启服务
stop        # 停止服务

# 测试
./demo_siliconflow.sh     # 完整演示
test_api                  # API 测试

# 日志
tail -f /tmp/minecontext.log

# 数据库
sqlite3 ~/.screenpipe/db.sqlite    # Screenpipe 数据
```

### 文档索引
- **配置完成报告.md** - 当前状态总结
- **QUICK_START.md** - 快速开始
- **SILICONFLOW_CONFIG.md** - LLM 配置
- **MineDesk_PRD_v1.6.md** - 产品需求
- **下一步行动计划.md** - 本文档

### 关键 URL
- MineContext API: http://127.0.0.1:17860
- API 文档: http://127.0.0.1:17860/docs
- 健康检查: http://127.0.0.1:17860/api/debug/health

---

## 💡 建议

### 如果您是...

**产品经理/规划者**：
→ 优先完成阶段 1（验证功能）  
→ 然后规划详细的功能需求  
→ 为前端开发做准备

**开发者**：
→ 立即开始阶段 1 和阶段 2  
→ 熟悉代码库结构  
→ 尝试小功能改进

**研究者/用户**：
→ 运行演示，体验功能  
→ 导入您的文档  
→ 测试实际使用场景

### 时间分配建议

**本周（5-8 小时）**：
- 阶段 1：3 小时
- 阶段 2：4 小时
- 规划：1 小时

**下周（10-15 小时）**：
- 前端评估：3 小时
- Screenpipe 集成：5 小时
- CrewAI 原型：5 小时
- 测试优化：2 小时

---

## ✅ 行动建议

**现在就做**（5 分钟）：
```bash
cd "/Users/ruiwang/Desktop/killer app"
./demo_siliconflow.sh
```

**今天完成**（1-2 小时）：
- 导入测试文档
- 体验 RAG 问答
- 浏览 API 文档

**本周完成**（4-6 小时）：
- 启动 Screenpipe
- 查看采集数据
- 规划前端开发

---

**祝开发顺利！🚀**

有任何问题随时查阅文档或查看日志。

